apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic-visits-service
  namespace: petclinic-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      imagePullSecrets:
        - name: ecr-secret
      initContainers:
        - name: wait-for-discovery
          image: busybox
          command: ['sh', '-c', 'until wget -q http://spring-petclinic-discovery-server:8761/actuator/health; do echo waiting; sleep 5; done']
      containers:
        - name: visits-service
          image: 118821711881.dkr.ecr.af-south-1.amazonaws.com/spring-petclinic-g11-visits-service:latest
          ports:
            - containerPort: 8082
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-visits-service
  namespace: petclinic-staging
spec:
  selector:
    app: visits-service
  ports:
    - port: 8082
      targetPort: 8082
